[project]
name = "dbt-olist"
version = "0.1.0"
description = "Projeto para treinar DBT"
authors = [
    {name = "kennedylsodre",email = "klacerda88@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "kagglehub (>=1.0.2,<2.0.0)",
    "python-dotenv (>=1.2.2,<2.0.0)",
    "kaggle (>=2.2.4,<3.0.0)",
    "google-cloud-bigquery (>=3.42.2,<4.0.0)",
    "dbt-bigquery (>=1.12.0,<2.0.0)",
    "google-cloud-storage (>=2.4,<3.2)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
